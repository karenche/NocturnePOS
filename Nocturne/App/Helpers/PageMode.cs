namespace Nocturne.App.Helpers
{
    /// <summary>
    /// Used to provide view, edit and add functions together in one page.
    /// </summary>
    public enum PageMode
    {
        View = 0,
        Edit = 1,
        Add = 2
    }
}
